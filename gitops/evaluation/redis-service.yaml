apiVersion: v1
kind: Service
metadata:
  name: evaluation-redis
  namespace: togglemaster
spec:
  selector:
    app: evaluation-redis
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP